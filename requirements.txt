faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0
pynput>=1.7.6
pyperclip>=1.8.2
ttkbootstrap>=1.20.0
pillow>=10.0.0
websockets>=12.0
# Use the OS cert store for TLS (works behind corporate SSL inspection); the
# entry points call truststore.inject_into_ssl() before any network import.
truststore>=0.9.0
